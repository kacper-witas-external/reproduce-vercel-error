{
  "name": "reproduce-error",
  "workspaces": [
    "landing-page/ui"
  ],
  "private": true,
  "scripts": {
    "build": "npm run build -ws --if-present",
    "start": "npm run start -ws --if-present"
  }
}
